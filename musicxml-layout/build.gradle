apply plugin: 'java'
sourceCompatibility = 6

dependencies {
	compile project(':layout')
	compile project(':musicxml-in')
}

sourceSets {
	main {
		java { srcDir 'src' }
		resources { srcDir 'src' }
	}
}