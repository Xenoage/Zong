apply plugin: 'java'
sourceCompatibility = 8

dependencies {
	compile project(':renderer')
	compile project(':utils-jse8')
}

sourceSets {
	main {
		java { srcDir 'src' }
		resources { srcDir 'src' }
	}
}