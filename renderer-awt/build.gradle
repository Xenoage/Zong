apply plugin: 'java'
sourceCompatibility = 8

dependencies {
	compile project(':renderer')
	compile project(':utils-jse')
	compile project(':utils-jsebase')
}

sourceSets {
	main {
		java { srcDir 'src' }
		resources { srcDir 'src' }
	}
}