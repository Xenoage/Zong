apply plugin: 'java'
sourceCompatibility = 8

dependencies {
	compile group: 'junit', name: 'junit', version: '4.+'
	compile project(':demos'), project(':webserver')
	compile 'com.googlecode.cedar-common:cpsuite:1.2.6'
}

test {
	exclude '**/MusicXmlMassTest.class'
}

sourceSets {
	test {
		java {
			srcDir 'test'
			srcDir 'nightly'
		}
		resources {
			srcDir 'test'
			srcDir 'nightly'
		}
	}
}

